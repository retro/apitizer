requirejs(['can/construct', 'can/util/fixture', 'tv4', './lib/types'], function(Construct, fixture, tv4, types){

	var Apitizer = Construct.extend({
		init : function(){
			
		}
	})

	console.log(types)

	return Apitizer;
})